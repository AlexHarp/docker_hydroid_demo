webapi:
  build: hydroid/api
  ports:
   - "9090:9090"
  links:
    - db
    - solr
    - jena
db:
  build: hydroid/postgres
  environment:
    POSTGRES_USER: hydroid
    POSTGRES_PASSWORD: postgres
solr:
  build: hydroid/solr
  ports:
   - "8983:8983"
jena:
  build: hydroid/jena
  ports:
   - "3030:3030"
